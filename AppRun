#!/usr/bin/env bash

set -e

HERE="$(readlink -f "$(dirname "$0")")"

# cd to the AppImage root so that the relative DATAPATH compiled into the
# binary (./usr/share/moon-lander/) resolves correctly.
cd "$HERE"

exec "$HERE/usr/games/moon-lander" "$@"
